%h2
  = link_to l(:label_rating_plural), :action => :index
  &#187; 
  =h @rating

- labelled_form_for @rating do |f|
  = render :partial => 'form', :locals => {:f => f}
  = submit_tag l(:button_save)

